app:
    resource: '@AppBundle/Controller/'
    type:     annotation

tasks:
    type:     rest
    resource: AppBundle\Controller\TaskController

api:
    resource: '.'
    type: 'api_platform'
    prefix: '/api'
